# ansible-role-netbox-bootstrap — sync TODO

Generated by `ansible-sync-role` on 2026-06-05.

---

## What was done

### Step 1 — Config files synced

| File | Action |
|---|---|
| `.ansible-lint` | **Overwritten** — added full enable_list, warn_list, loop_var_prefix, fqcn[builtins] (was fqcn-builtins), mock_roles/mock_modules |
| `.gitignore` | **Overwritten** — aligned to template (added .ansible/, .cache/, molecule/.cache/, zzz.diff) |
| `.pre-commit-config.yaml` | **Overwritten** — bumped ruff to v0.15.14, added hadolint and jumanjihouse (shellcheck/shfmt) blocks |
| `.yamllint` | **Kept** — functionally identical to template; only missing two explanatory comments |
| `ruff.toml` | **Added** — was missing entirely |

⚠️ `.env` was removed from `.gitignore` when it was overwritten from the template. `scripts/.env` must remain gitignored. Add it back:

```bash
echo "scripts/.env" >> .gitignore
git add .gitignore
```

### Step 1b — Legacy CI files
No `.travis.yml`, `Jenkinsfile`, or `.circleci/` found. Nothing to do.

### Step 2 — CLAUDE.md
**Kept** — existing CLAUDE.md is more detailed and role-specific than the template would generate. It already contains all relevant content including the NetBox-specific commit guide.

### Step 3 — ansible-core 2.20 compliance
Added `no_log: true` to 8 tasks that were missing it:

- `aggregates.yml` — "Ensure aggregates exist" (uri with Authorization header)
- `custom_links.yml` — "Fetch existing custom links" and "Ensure custom links exist"
- `export_templates.yml` — "Fetch existing export templates" and "Ensure export templates exist"
- `groups.yml` — "Ensure groups exist"
- `virtual_machine_types.yml` — "Ensure virtual_machine_types exist"
- `users.yml` — "Ensure local users exist" (has `password:` field)

No `include_vars` path issues, no `loop_var:` custom names, no injected-fact usage.

### Step 4 — Lint clean
No `argument_specs.yml` exists. No `files/` or `templates/` directories. All modules use FQCNs. No changes needed.

### Step 5 — meta/main.yml
- Added `issue_tracker_url: https://github.com/basictheprogram/ansible-role-netbox-bootstrap/issues`
- Fixed multiline `description:` YAML formatting
- Added Ubuntu `jammy` to platforms (supported until 2027-04-30)
- Expanded `galaxy_tags` with: `debian`, `ubuntu`, `tenancy`, `users`, `permissions`, `webhooks`, `customfields`, `virtualization`

### Step 6 — defaults/ vs vars/
`defaults/main.yml` only — no OS-specific files to migrate. No changes needed.

### Step 7 — Preflight assertions
`tasks/preflight.yml` already exists and is comprehensive: Ansible version check, netbox_url validation, API connectivity test, NetBox version check, collection presence check. No changes needed.

### Step 8 — README
Added **Supported Platforms** section (Debian bookworm/trixie, Ubuntu jammy/noble/resolute) between Requirements and Role Variables.

### Steps 9–13 — Molecule

Created from scratch:

- `molecule/default/molecule.yml` — 5-platform matrix (bookworm, trixie, jammy, noble, resolute), testinfra verifier
- `molecule/default/converge.yml` — self-contained, ansible_facts-based cache pre_tasks
- `molecule/default/group_vars/all/main.yml` — all sections disabled (no live NetBox needed)
- `molecule/default/tests/__init__.py`
- `molecule/default/tests/conftest.py`
- `molecule/default/tests/test_python.py` — Python 3 ≥ 3.10 check
- `molecule/default/tests/test_ansible.py` — Ansible ≥ 2.20 check
- `molecule/requirements.txt`

---

## Open items

### 🔴 Must fix before committing

1. **Re-add `scripts/.env` to `.gitignore`** — was dropped when the template was applied.

   ```bash
   echo "scripts/.env" >> .gitignore
   ```

### 🟡 Follow-up work

2. **Molecule full integration tests** — the current molecule scenario only verifies that Python 3 and Ansible ≥ 2.20 are present on the control-node container. It does not exercise any NetBox API calls because all sections are disabled. To add real integration tests, start a NetBox container as a molecule dependency and enable sections with a test API token. See DESIGN.md open questions.

3. **`molecule/default/requirements.yml`** — molecule's galaxy dependency resolver needs a `requirements.yml` listing the `netbox.netbox` collection. Create it:

   ```yaml
   ---
   collections:
     - name: netbox.netbox
     - name: community.general
   ```

4. **`meta/argument_specs.yml`** — does not exist. Consider adding it to document every variable in `defaults/main.yml`. Ansible lint's `meta[no-info]` is currently satisfied by `meta/main.yml` but `argument_specs.yml` enables better IDE support and validation.

5. **Open questions from DESIGN.md** (unchanged):
   - Config contexts (`/api/extras/config-contexts/`) — deferred to Tier 6
   - Multi-instance Click profiles (`--profile production`) — future subcommand
   - Scheduled export mechanism (cron vs scheduled task)

6. **`.ansible-lint` mock entries** — the overwritten `.ansible-lint` includes `mock_roles` (jborean93.win_openssh) and `mock_modules` for Docker/Windows that don't apply to this role. They're harmless but could be cleaned up:

   ```yaml
   mock_roles: []
   mock_modules: []
   ```
