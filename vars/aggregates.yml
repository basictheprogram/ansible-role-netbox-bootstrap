---
# aggregates — populated by scripts/export_netbox.py
# Do not hand-edit; re-run the export script to update.
# Add ISP-assigned public blocks (ARIN) after the role runs or via export.
aggregates:
  - prefix: "10.0.0.0/8"
    rir: RFC 1918
    description: "RFC 1918 — Class A private range"
  - prefix: "172.16.0.0/12"
    rir: RFC 1918
    description: "RFC 1918 — Class B private range"
  - prefix: "192.168.0.0/16"
    rir: RFC 1918
    description: "RFC 1918 — Class C private range"
